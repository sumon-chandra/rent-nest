enum Role {
   TENANT
   LANDLORD
   ADMIN
}

enum UserStatus {
   ACTIVE
   BANNED
}

enum PropertyStatus {
   AVAILABLE
   RENTED
   UNAVAILABLE
}

enum RentalRequestStatus {
   PENDING
   APPROVED
   REJECTED
   CANCELLED
   COMPLETED
}

enum PaymentProvider {
   STRIPE
   SSLCOMMERZ
}

enum PaymentStatus {
   PENDING
   COMPLETED
   FAILED
   REFUNDED
}
